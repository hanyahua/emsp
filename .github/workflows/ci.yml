name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Java 21
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '21'

      - name: Set up Maven
        uses: stCarolas/setup-maven@v4
        with:
          maven-version: '3.9.6'

      - name: Build with Maven
        run: mvn clean package

      - name: Prepare files for upload
        run: |
          cp target/*.jar emps.jar

      - name: Generate .env file
        run: |
          echo "MYSQL_ROOT_PASSWORD=${{ secrets.MYSQL_ROOT_PASSWORD }}" > .env
          echo "DATASOURCE_USERNAME=${{ secrets.DB_USERNAME }}" >> .env
          echo "DATASOURCE_PASSWORD=${{ secrets.DB_PASSWORD }}" >> .env

      - name: Upload jar and Dockerfile to EC2
        uses: appleboy/scp-action@v0.1.7
        with:
            host: ${{ secrets.EC2_HOST }}
            username: ${{ secrets.EC2_USER }}
            key: ${{ secrets.EC2_SSH_KEY }}
            source: |
              emps.jar
              Dockerfile
              docker-compose.yml
              .env
            target: /home/${{ secrets.EC2_USER }}/emps

      - name: Build and run Docker container on EC2
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USER }}
          key: ${{ secrets.EC2_SSH_KEY }}
          script: |
            cd /home/${{ secrets.EC2_USER }}/emps
            docker-compose down
            docker-compose up --build -d
